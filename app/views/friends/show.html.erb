
<div class="jumbotron">
  <h1 class="display-4"><%= render @friend %></h1>
  <br/>
  <%= link_to "Edit", edit_friend_path(@friend), class:"btn btn-secondary" %>
  <%= link_to "Back", friends_path, class:"btn btn-secondary" %>
  <%= button_to "Delete", @friend, data: { turbo_method: :delete, turbo_confirm: "Are you sure?"}, class:"btn btn-danger" %>
</div>

